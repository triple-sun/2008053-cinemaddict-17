import AbstractView from '../../../framework/view/abstract-view.js';

const createMoviePopupCommentsLoadingFailTemplate = () => ('<h3 class="film-details__comments-title">Loading comments...</h3>');

export default class MoviePopupCommentsLoadingFailView extends AbstractView {
  get template() {
    return createMoviePopupCommentsLoadingFailTemplate();
  }
}
